{
  "name": "isolation-levels-demo",
  "private": true,
  "packageManager": "pnpm@10.26.2",
  "scripts": {
    "dev": "pnpm build:shared && pnpm --parallel dev:run",
    "dev:backend": "pnpm build:shared && pnpm --filter backend start:dev",
    "dev:frontend": "pnpm --filter frontend dev",
    "build:shared": "pnpm --filter @isolation-demo/shared build",
    "lint": "pnpm --parallel lint",
    "build": "pnpm build:shared && pnpm --parallel build"
  },
  "devDependencies": {
    "prettier": "^3.7.4",
    "typescript": "^5.9.3"
  }
}
