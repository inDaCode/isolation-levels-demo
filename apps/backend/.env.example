# Database connection
# Defaults work with docker-compose.yml out of the box
# Customize if your port 5432 is already in use

DB_HOST=localhost
DB_PORT=5432
DB_NAME=isolation_demo
DB_USER=demo
DB_PASSWORD=demo